function greet(person: string): string {
    return `Hello ${person} , hello welcome`
}

const username: string = "deep";

// console.log(greet(username));

import { makeChai } from "./functionTs.js";
makeChai("elcachi" , 2);


